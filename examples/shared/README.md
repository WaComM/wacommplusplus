# Shared example data

Stage read-only forcing files needed by more than one experiment here. This directory contains no bundled forcing dataset. Each example guide names its required fields, units, provenance, and checksums; keep run-specific configurations and source definitions in that example directory. Configure paths explicitly before running from the repository root.

## References

See the scientific references in each example guide and the repository [model description](../../docs/model.md).
